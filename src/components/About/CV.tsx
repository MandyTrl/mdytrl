import { Collapse } from '../UI/Collapse'

export const CV = () => {
  return (
    <section id="CV">
      <Collapse titre={'Formation'} />
      <Collapse titre={'Parcours professionnel'} />
    </section>
  )
}
